class Cotizador {
    constructor(factorprodPerf, factorprodFarm,) {
        this.factorPp = parseFloat(factorprodPerf)
        this.factorPf = parseFloat(factorprodFarm)
        this.desc1 = parseFloat(desc1)
    
    }
    cotizar() {
        let precio = parseFloat (this.desc1 * this.factorPp)
            return precio.toLocaleString()}
            cotizar() {
                let precio = parseFloat (this.desc1 * this.factorPf)
                    return precio.toLocaleString()}
        
    

        
        }

    